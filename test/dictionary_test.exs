defmodule Unabbreviated.DictionaryTest do
  use ExUnit.Case
  alias Unabbreviated.Dictionary

  describe "returns the correct result" do
    #test "for 1" do
    #  assert Dictionary.convert("1") == "um"
    #end

    #test "for 5" do
    #  assert Dictionary.convert("5") == "cinco"
    #end

    #test "for 100" do
    #  assert Dictionary.convert("100") == "cem"
    #end
  end
end
